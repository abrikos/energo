import React from "react";
import { Route } from "react-router-dom";

const routes = (
	<React.Fragment>
		<Route path="/admin/analytics" component={() => <div> ala </div>} />
	</React.Fragment>
);

export default routes;
